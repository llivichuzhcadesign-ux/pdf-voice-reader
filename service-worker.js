const CACHE_NAME = 'orbit-pwa-v15';
const APP_SHELL = ['./','./index.html','./styles.css','./app.js','./manifest.webmanifest','./vendor/pdf.min.mjs','./vendor/pdf.worker.min.mjs'];

const ORBIT_VIEWER_CSS = `
.unit.current{background:transparent!important;color:#fff!important;border-radius:0!important;box-shadow:inset 0 -.16em 0 rgba(220,172,66,.72)!important;outline:1px solid rgba(220,172,66,.42)!important;outline-offset:.08em!important}
body.pdf-inverted .unit.current{box-shadow:inset 0 -.16em 0 rgba(220,172,66,.78)!important}
.readerGrid{align-items:stretch!important}.pdfPreview,.textReader{min-height:0!important;overflow:hidden!important}.pdfPreview{background:linear-gradient(180deg,rgba(18,18,18,.74),rgba(5,5,5,.9))!important}
.orbitPdfToolbar{display:flex;align-items:center;gap:.55rem;margin-left:auto}.orbitPdfButton,.orbitPdfChip{height:2.1rem;min-width:2.1rem;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.045);color:#f7f7f7;border-radius:.58rem;font:800 .86rem/1 system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;display:inline-flex;align-items:center;justify-content:center;padding:0 .7rem}.orbitPdfButton:active{transform:translateY(1px)}
.orbitPdfViewer{position:relative;flex:1 1 auto;min-height:0;height:100%;overflow:auto;padding:1.2rem 0 7rem;background:radial-gradient(circle at 50% 0%,rgba(255,255,255,.045),transparent 28%),#050505;border-top:1px solid rgba(255,255,255,.08);scrollbar-color:rgba(255,255,255,.25) transparent}.orbitPdfStage{width:max-content;min-width:100%;margin:0 auto;display:flex;flex-direction:column;align-items:center;gap:1.2rem}.orbitPdfPage{position:relative;background:#000;box-shadow:0 0 0 1px rgba(255,255,255,.09),0 22px 54px rgba(0,0,0,.55);overflow:visible}.orbitPdfPage.is-active{box-shadow:0 0 0 2px rgba(220,172,66,.82),0 0 0 6px rgba(220,172,66,.12),0 26px 64px rgba(0,0,0,.62)}.orbitPdfPage canvas{display:block;width:100%;height:100%;filter:invert(1) hue-rotate(180deg)}.orbitPdfViewer:not(.inverted) .orbitPdfPage canvas{filter:none}.orbitReadingBadge{position:absolute;top:.8rem;left:1rem;z-index:2;color:#f3d16a;border:1px solid rgba(220,172,66,.78);background:rgba(0,0,0,.72);border-radius:999px;padding:.36rem .74rem;font-weight:900;font-size:.82rem;box-shadow:0 0 22px rgba(220,172,66,.14)}.orbitPdfEmpty,.orbitPdfLoading{min-height:58vh;display:grid;place-items:center;color:rgba(255,255,255,.55);font-weight:800}body[data-reader-pane='pdf'] .orbitPdfViewer{min-height:calc(100dvh - 15rem)}
@media(max-width:700px){body[data-reader-pane='pdf'] .orbitPdfViewer{height:calc(100dvh - 15.5rem);min-height:calc(100dvh - 15.5rem)}.orbitPdfViewer{padding:.8rem 0 7rem}.orbitPdfToolbar{gap:.35rem}.orbitPdfButton,.orbitPdfChip{height:2rem;min-width:2rem;padding:0 .55rem}body[data-mobile-view='reader'][data-reader-pane='pdf'] main{height:calc(100dvh - 96px - env(safe-area-inset-bottom))!important}body[data-reader-pane='pdf'] #toolbar{min-height:58px!important;padding:8px 10px!important;border-bottom-color:rgba(120,130,142,.12)!important;box-shadow:0 8px 22px rgba(0,0,0,.22)!important}body[data-reader-pane='pdf'] .mobileTopLine{display:grid!important;grid-template-columns:auto minmax(0,1fr)!important;gap:8px!important;align-items:center!important}body[data-reader-pane='pdf'] .mobileTopLine strong{display:block!important;font-size:24px!important;line-height:1!important;letter-spacing:-.02em!important;white-space:nowrap!important}body[data-reader-pane='pdf'] #mobileDocTitle{display:none!important}body[data-reader-pane='pdf'] .mobileReaderTools{justify-content:flex-end!important;gap:6px!important;min-width:0!important}body[data-reader-pane='pdf'] .paneSwitch{grid-template-columns:repeat(2,minmax(42px,1fr))!important;gap:2px!important;padding:2px!important}body[data-reader-pane='pdf'] .paneSwitch button{height:32px!important;padding:0 9px!important;font-size:11px!important}body[data-reader-pane='pdf'] #mobileInvertBtn,body[data-reader-pane='pdf'] #mobileSettingsBtn{height:36px!important;min-width:36px!important}body[data-reader-pane='pdf'] #mobileInvertBtn{padding:0 10px!important}body[data-reader-pane='pdf'] #viewer{height:calc(100dvh - 154px - env(safe-area-inset-bottom))!important;padding:0 0 calc(96px + env(safe-area-inset-bottom))!important;overflow:hidden!important}body[data-reader-pane='pdf'] .readerGrid,body[data-reader-pane='pdf'] .pdfPreview{height:100%!important;min-height:0!important;gap:0!important}body[data-reader-pane='pdf'] .pdfPreview{border-radius:0!important;border-left:0!important;border-right:0!important;border-bottom:0!important;box-shadow:none!important;background:#050505!important}body[data-reader-pane='pdf'] .pdfPreview h3{display:flex!important;align-items:center!important;min-height:52px!important;padding:8px!important;overflow-x:auto!important;overflow-y:hidden!important;scrollbar-width:none!important;background:rgba(11,13,15,.92)!important}body[data-reader-pane='pdf'] .pdfPreview h3::-webkit-scrollbar{display:none!important}body[data-reader-pane='pdf'] .pdfPreview h3>span:first-child{display:none!important}body[data-reader-pane='pdf'] .orbitPdfToolbar{width:100%!important;margin:0!important;gap:5px!important;justify-content:space-between!important;flex-wrap:nowrap!important}body[data-reader-pane='pdf'] .orbitPdfButton,body[data-reader-pane='pdf'] .orbitPdfChip{height:36px!important;min-width:38px!important;border-radius:12px!important;padding:0 9px!important;font-size:15px!important;flex:0 0 auto!important;white-space:nowrap!important}body[data-reader-pane='pdf'] #orbitPdfPageLabel{min-width:74px!important}body[data-reader-pane='pdf'] #orbitPdfZoomLabel{min-width:54px!important}body[data-reader-pane='pdf'] #orbitPdfFit,body[data-reader-pane='pdf'] #orbitPdfActual{min-width:48px!important}body[data-reader-pane='pdf'] .orbitPdfViewer{height:calc(100% - 52px)!important;min-height:0!important;padding:.55rem 0 calc(7rem + env(safe-area-inset-bottom))!important;border-top-color:rgba(255,255,255,.06)!important}body[data-reader-pane='pdf'] .orbitPdfStage{gap:.8rem!important}body[data-reader-pane='pdf'] .orbitPdfPage{box-shadow:0 0 0 1px rgba(220,172,66,.18),0 18px 42px rgba(0,0,0,.5)!important}body[data-reader-pane='pdf'] .orbitReadingBadge{top:.55rem!important;left:.6rem!important;font-size:.76rem!important;padding:.32rem .62rem!important}}
`;

const LIBRARY_GALLERY_CSS = `
#library{position:relative}.orbitLibraryToolbar{position:absolute;right:18px;top:18px;z-index:4;display:grid;grid-template-columns:repeat(3,36px);gap:3px;padding:3px;border:1px solid rgba(255,255,255,.13);border-radius:999px;background:linear-gradient(180deg,rgba(255,255,255,.08),rgba(255,255,255,.025));backdrop-filter:blur(14px)}.orbitLibraryViewBtn{width:36px;height:32px;min-width:0;padding:0;border:0;border-radius:999px;background:transparent;color:#9aa1aa;font-size:14px;box-shadow:none}.orbitLibraryViewBtn.active,.orbitLibraryViewBtn:hover{background:rgba(255,255,255,.14);color:#fff;transform:none}.orbitGalleryList{display:block!important;overflow:auto!important;padding:2px 0 8px!important}.orbitGallery{display:grid;grid-template-columns:1fr;gap:12px;min-height:0}.orbitGalleryStage{position:relative;display:grid;place-items:center;min-height:320px;perspective:1200px}.orbitGalleryStack{position:absolute;inset:0;display:grid;place-items:center}.orbitGalleryFeature,.orbitGalleryStackCover,.orbitGalleryRailItem{appearance:none;border:0;background:transparent;color:#fff;padding:0;box-shadow:none;text-align:left}.orbitGalleryFeature{position:relative;z-index:8;filter:drop-shadow(0 26px 46px rgba(0,0,0,.72))}.orbitGalleryStackCover{position:absolute;z-index:calc(7 - var(--stack-index));opacity:calc(.7 - (var(--stack-index) * .08));transform:translateX(calc(var(--stack-index) * 22px)) translateY(calc(var(--stack-index) * 5px)) rotateY(calc(var(--stack-index) * -12deg)) scale(calc(1 - (var(--stack-index) * .05)));transform-origin:left center}.orbitBookCover{position:relative;display:flex;flex-direction:column;justify-content:space-between;width:min(250px,62vw);aspect-ratio:3/4.15;border:1px solid rgba(255,255,255,.14);border-radius:14px;padding:18px 16px 14px 23px;overflow:hidden;background:linear-gradient(145deg,#202329 0%,#090a0c 48%,#000 100%);box-shadow:inset 0 1px 0 rgba(255,255,255,.06),inset 8px 0 22px rgba(255,255,255,.035)}.orbitBookCover::before{content:'';position:absolute;inset:0;z-index:1;background:radial-gradient(circle at 24% 14%,rgba(255,255,255,.16),transparent 22%),linear-gradient(130deg,transparent 0 42%,rgba(255,255,255,.08) 44%,transparent 50%);pointer-events:none}.orbitBookCover::after{content:'';position:absolute;inset:10px;border:1px solid rgba(255,255,255,.06);border-radius:10px;pointer-events:none}.orbitBookCover.marked{border-color:rgba(255,43,43,.5)}.orbitBookCover.stack{width:min(224px,56vw)}.orbitBookCover.rail{width:112px;min-width:112px;border-radius:10px;padding:12px 10px 10px 15px}.orbitBookArt{position:absolute;z-index:0;inset:16px 15px 58px 25px;border-radius:8px;background:linear-gradient(180deg,rgba(255,255,255,.12),rgba(255,255,255,.03));opacity:.36;box-shadow:0 0 0 1px rgba(255,255,255,.08),inset 0 0 42px rgba(0,0,0,.7)}.orbitBookCover[data-cover-ready='true'] .orbitBookArt{background-image:linear-gradient(180deg,rgba(0,0,0,.05),rgba(0,0,0,.58)),var(--cover-image);background-size:cover;background-position:top center;opacity:.84;filter:grayscale(1) contrast(1.05) brightness(.86)}.orbitBookCover.rail .orbitBookArt{inset:10px 9px 38px 14px;border-radius:6px}.orbitBookSpine{position:absolute;z-index:2;left:0;top:0;bottom:0;width:12px;background:linear-gradient(180deg,rgba(255,255,255,.13),rgba(255,255,255,.02));border-right:1px solid rgba(255,255,255,.08)}.orbitBookSource{position:relative;z-index:4;align-self:flex-start;border:1px solid rgba(255,255,255,.14);border-radius:999px;padding:5px 8px;color:#d4d4d4;font-size:10px;font-weight:900;text-transform:uppercase;letter-spacing:.06em;background:rgba(0,0,0,.48)}.orbitBookCover strong{position:relative;z-index:4;display:block;margin-top:28px;color:#fff;font-size:18px;line-height:1.08;font-weight:920;letter-spacing:-.02em;overflow-wrap:anywhere;text-shadow:0 2px 14px rgba(0,0,0,.88)}.orbitBookCover.rail strong{font-size:10px}.orbitBookProgress{position:relative;z-index:4;display:flex;align-items:center;gap:7px;margin-top:18px;color:#d7d7d7;font-size:11px;font-weight:820;line-height:1.25;text-shadow:0 2px 10px rgba(0,0,0,.85)}.orbitBookProgress b{width:7px;height:7px;border-radius:50%;background:#ff2b2b;box-shadow:0 0 14px rgba(255,43,43,.55)}.orbitGalleryInfo{padding:0 4px}.orbitGalleryInfo h3{margin:0 0 7px;color:#fff;font-size:28px;line-height:.95;font-weight:940;letter-spacing:-.04em;overflow-wrap:anywhere}.orbitGalleryInfo p{margin:0;color:#bfc4ca;font-size:13px;font-weight:820}.orbitGalleryRail{display:flex;gap:12px;overflow:auto;padding:8px 2px 4px;scrollbar-width:none;mask-image:linear-gradient(90deg,transparent,#000 18px,#000 calc(100% - 18px),transparent)}.orbitGalleryRail::-webkit-scrollbar{display:none}.orbitGalleryRailItem{flex:0 0 auto;border-radius:13px;transition:transform .16s ease,opacity .16s ease}.orbitGalleryRailItem.active,.orbitGalleryRailItem:hover{transform:translateY(-4px);opacity:1}.orbitGalleryRailItem:not(.active){opacity:.66}@media(min-width:701px){.orbitGallery{grid-template-columns:minmax(300px,42vw) minmax(220px,1fr);grid-template-areas:'stage info' 'rail rail';align-items:center}.orbitGalleryStage{grid-area:stage;min-height:430px}.orbitGalleryInfo{grid-area:info}.orbitGalleryRail{grid-area:rail}.orbitBookCover{width:min(330px,42vw)}.orbitBookCover.stack{width:min(292px,36vw)}}@media(max-width:700px){.orbitLibraryToolbar{right:14px;top:calc(18px + env(safe-area-inset-top))}body[data-mobile-view='library'] #library{height:calc(100dvh - 96px - env(safe-area-inset-bottom))!important}.orbitGalleryList{height:calc(100% - 116px)!important;padding-top:8px!important}.orbitGalleryStage{min-height:300px}.orbitGalleryInfo p{display:none}}
`;

const ORBIT_VIEWER_JS = `
;(() => {
  let orbitCurrentPdfBuffer = null;
  let orbitPdfResizeTimer = null;
  let orbitPdfState = { document: null, pages: [], pageCount: 0, currentPage: 1, targetPage: null, scale: 1, fitMode: true, baseWidth: 760, baseHeight: 980, rendering: new Set(), rendered: new Map(), destroyed: false };
  function orbitPdfOptions(buffer) { return { data: buffer.slice(0), cMapUrl: new URL('./vendor/cmaps/', import.meta.url).toString(), cMapPacked: true, standardFontDataUrl: new URL('./vendor/standard_fonts/', import.meta.url).toString() }; }
  function orbitResetState() { orbitPdfState = { document: null, pages: [], pageCount: 0, currentPage: 1, targetPage: null, scale: 1, fitMode: true, baseWidth: 760, baseHeight: 980, rendering: new Set(), rendered: new Map(), destroyed: false }; }
  function orbitCleanupPdfViewer() { orbitPdfState.destroyed = true; if (orbitPdfResizeTimer) clearTimeout(orbitPdfResizeTimer); if (orbitPdfState.document) { try { orbitPdfState.document.destroy(); } catch (error) {} } orbitResetState(); }
  function orbitApplyInvertState() { const on = settings.pdfInverted !== false; document.body.classList.toggle('pdf-inverted', on); document.querySelectorAll('.pdfEmbed, .orbitPdfViewer').forEach((el) => el.classList.toggle('inverted', on)); [invertBtn, mobileInvertBtn].forEach((btn) => { if (!btn) return; btn.textContent = on ? 'Invert On' : 'Invert Off'; btn.setAttribute('aria-pressed', String(on)); }); }
  if (typeof applyInvertState === 'function') applyInvertState = orbitApplyInvertState;
  const orbitOriginalResetViewer = resetViewer; resetViewer = function(...args) { orbitCleanupPdfViewer(); orbitCurrentPdfBuffer = null; return orbitOriginalResetViewer.apply(this, args); };
  const orbitOriginalLoadPdf = loadPdf; loadPdf = async function(id) { try { const rec = await getPdf(id); orbitCurrentPdfBuffer = rec && rec.buffer ? rec.buffer : null; } catch (error) { orbitCurrentPdfBuffer = null; } return orbitOriginalLoadPdf.call(this, id); };
  function orbitPageWidth() { const viewer = document.getElementById('orbitPdfViewer'); return viewer ? Math.max(280, viewer.clientWidth - 32) : 760; }
  function orbitUpdateToolbar() { const page = document.getElementById('orbitPdfPageLabel'); const zoom = document.getElementById('orbitPdfZoomLabel'); if (page) page.textContent = String(orbitPdfState.currentPage) + ' / ' + String(Math.max(orbitPdfState.pageCount, 1)); if (zoom) zoom.textContent = Math.round(orbitPdfState.scale * 100) + '%'; }
  function orbitAttachPdfControls() { const prev = document.getElementById('orbitPdfPrev'); const next = document.getElementById('orbitPdfNext'); const out = document.getElementById('orbitPdfZoomOut'); const inBtn = document.getElementById('orbitPdfZoomIn'); const fit = document.getElementById('orbitPdfFit'); const one = document.getElementById('orbitPdfActual'); const viewer = document.getElementById('orbitPdfViewer'); if (prev) prev.onclick = () => orbitScrollToPage(Math.max(1, orbitPdfState.currentPage - 1)); if (next) next.onclick = () => orbitScrollToPage(Math.min(orbitPdfState.pageCount || 1, orbitPdfState.currentPage + 1)); if (out) out.onclick = () => orbitSetZoom(orbitPdfState.scale - .15, false); if (inBtn) inBtn.onclick = () => orbitSetZoom(orbitPdfState.scale + .15, false); if (fit) fit.onclick = () => { orbitPdfState.fitMode = true; orbitLayoutPdfPages(true); }; if (one) one.onclick = () => orbitSetZoom(1, false); if (viewer) viewer.onscroll = () => { orbitUpdateCurrentPageFromScroll(); orbitRenderVisiblePages(); }; }
  function orbitSetZoom(nextScale, fitMode) { orbitPdfState.fitMode = !!fitMode; orbitPdfState.scale = Math.min(3.5, Math.max(.35, nextScale)); orbitLayoutPdfPages(true); }
  function orbitLayoutPdfPages(preserve) { const viewer = document.getElementById('orbitPdfViewer'); if (!viewer || !orbitPdfState.pages.length) return; const previousPage = preserve ? orbitPdfState.currentPage : 1; if (orbitPdfState.fitMode) orbitPdfState.scale = Math.min(3.5, Math.max(.35, orbitPageWidth() / orbitPdfState.baseWidth)); orbitPdfState.rendered.clear(); orbitPdfState.pages.forEach((item) => { item.scale = orbitPdfState.scale; item.width = Math.round(item.baseWidth * item.scale); item.height = Math.round(item.baseHeight * item.scale); item.shell.style.width = item.width + 'px'; item.shell.style.height = item.height + 'px'; item.canvas.style.width = item.width + 'px'; item.canvas.style.height = item.height + 'px'; item.canvas.width = 1; item.canvas.height = 1; }); orbitUpdateToolbar(); requestAnimationFrame(() => { orbitScrollToPage(previousPage, 'auto'); orbitRenderVisiblePages(); }); }
  function orbitHandleResize() { if (!orbitPdfState.fitMode) return; clearTimeout(orbitPdfResizeTimer); orbitPdfResizeTimer = setTimeout(() => orbitLayoutPdfPages(true), 120); }
  async function orbitRenderPdf(buffer) { const viewer = document.getElementById('orbitPdfViewer'); if (!viewer || !buffer) return; orbitCleanupPdfViewer(); orbitPdfState.destroyed = false; viewer.innerHTML = '<div class="orbitPdfLoading">Preparing PDF</div>'; try { const pdf = await pdfjsLib.getDocument(orbitPdfOptions(buffer)).promise; if (orbitPdfState.destroyed) return; orbitPdfState.document = pdf; orbitPdfState.pageCount = pdf.numPages; const firstPage = await pdf.getPage(1); const firstViewport = firstPage.getViewport({ scale: 1 }); orbitPdfState.baseWidth = firstViewport.width || 760; orbitPdfState.baseHeight = firstViewport.height || 980; viewer.innerHTML = '<div class="orbitPdfStage" id="orbitPdfStage"></div>'; const stage = document.getElementById('orbitPdfStage'); for (let pageNum = 1; pageNum <= pdf.numPages; pageNum += 1) { const shell = document.createElement('div'); shell.className = 'orbitPdfPage'; shell.dataset.page = String(pageNum); shell.innerHTML = '<canvas aria-label="Page ' + pageNum + '"></canvas>'; stage.appendChild(shell); orbitPdfState.pages.push({ pageNum, page: pageNum === 1 ? firstPage : null, baseWidth: orbitPdfState.baseWidth, baseHeight: orbitPdfState.baseHeight, shell, canvas: shell.querySelector('canvas'), scale: 1, width: orbitPdfState.baseWidth, height: orbitPdfState.baseHeight }); } orbitAttachPdfControls(); orbitLayoutPdfPages(false); window.addEventListener('resize', orbitHandleResize, { passive: true }); } catch (error) { viewer.innerHTML = '<div class="orbitPdfEmpty">PDF preview unavailable</div>'; } }
  function orbitRenderVisiblePages() { const viewer = document.getElementById('orbitPdfViewer'); if (!viewer || !orbitPdfState.pages.length) return; const top = viewer.scrollTop - viewer.clientHeight * .7; const bottom = viewer.scrollTop + viewer.clientHeight * 1.7; orbitPdfState.pages.forEach((item) => { const offset = item.shell.offsetTop; if (offset + item.height >= top && offset <= bottom) orbitRenderPdfPage(item); }); }
  async function orbitRenderPdfPage(item) { const key = item.pageNum + ':' + item.scale; if (orbitPdfState.rendered.get(item.pageNum) === key || orbitPdfState.rendering.has(key)) return; orbitPdfState.rendering.add(key); try { if (!item.page) item.page = await orbitPdfState.document.getPage(item.pageNum); const dpr = Math.min(window.devicePixelRatio || 1, 2); const viewport = item.page.getViewport({ scale: item.scale * dpr }); item.canvas.width = Math.round(viewport.width); item.canvas.height = Math.round(viewport.height); item.canvas.style.width = item.width + 'px'; item.canvas.style.height = item.height + 'px'; const context = item.canvas.getContext('2d', { alpha: false }); await item.page.render({ canvasContext: context, viewport }).promise; orbitPdfState.rendered.set(item.pageNum, key); } catch (error) {} finally { orbitPdfState.rendering.delete(key); } }
  function orbitUpdateCurrentPageFromScroll() { const viewer = document.getElementById('orbitPdfViewer'); if (!viewer || !orbitPdfState.pages.length) return; const anchor = viewer.scrollTop + viewer.clientHeight * .34; let current = orbitPdfState.pages[0].pageNum; orbitPdfState.pages.forEach((item) => { if (item.shell.offsetTop <= anchor) current = item.pageNum; }); orbitPdfState.currentPage = current; orbitMarkActivePage(current); orbitUpdateToolbar(); }
  function orbitMarkActivePage(pageNum) { orbitPdfState.pages.forEach((item) => { item.shell.classList.toggle('is-active', item.pageNum === pageNum); let badge = item.shell.querySelector('.orbitReadingBadge'); if (item.pageNum === pageNum) { if (!badge) { badge = document.createElement('div'); badge.className = 'orbitReadingBadge'; item.shell.appendChild(badge); } badge.textContent = 'Reading page ' + pageNum; } else if (badge) badge.remove(); }); }
  function orbitScrollToPage(pageNum, behavior) { const viewer = document.getElementById('orbitPdfViewer'); const item = orbitPdfState.pages[Math.max(0, Math.min(pageNum - 1, orbitPdfState.pages.length - 1))]; if (!viewer || !item) return; orbitPdfState.currentPage = item.pageNum; orbitMarkActivePage(item.pageNum); orbitUpdateToolbar(); viewer.scrollTo({ top: Math.max(0, item.shell.offsetTop - 14), behavior: behavior || 'smooth' }); orbitRenderVisiblePages(); }
  window.setOrbitPdfActivePage = function(pageNum, options) { const target = Number(pageNum); if (!Number.isFinite(target) || target <= 0) return; orbitPdfState.targetPage = target; orbitPdfState.currentPage = Math.min(Math.max(1, Math.round(target)), Math.max(orbitPdfState.pageCount, 1)); orbitMarkActivePage(orbitPdfState.currentPage); orbitUpdateToolbar(); if (options && options.scroll) orbitScrollToPage(orbitPdfState.currentPage, options.behavior || 'smooth'); };
  renderReader = function(name, pdfUrl, extractedText) { const viewer = $('viewer'); viewer.innerHTML = ''; const grid = document.createElement('div'); grid.className = 'readerGrid'; grid.innerHTML = '<div class="pdfPreview"><h3><span>PDF preview</span><span class="orbitPdfToolbar"><button class="orbitPdfButton" id="orbitPdfPrev" type="button" aria-label="Previous page">‹</button><span class="orbitPdfChip" id="orbitPdfPageLabel">1 / 1</span><button class="orbitPdfButton" id="orbitPdfNext" type="button" aria-label="Next page">›</button><button class="orbitPdfButton" id="orbitPdfZoomOut" type="button" aria-label="Zoom out">−</button><span class="orbitPdfChip" id="orbitPdfZoomLabel">100%</span><button class="orbitPdfButton" id="orbitPdfZoomIn" type="button" aria-label="Zoom in">+</button><button class="orbitPdfButton" id="orbitPdfFit" type="button">Fit</button><button class="orbitPdfButton" id="orbitPdfActual" type="button">100</button></span></h3><div class="orbitPdfViewer inverted" id="orbitPdfViewer"></div></div><div class="textReader"><h3>Read-aloud text <span id="modeBadge" class="modeBadge"></span></h3><div class="textContent" id="textContent"></div></div>'; viewer.appendChild(grid); renderTextPane(extractedText); applyInvertState(); if (orbitCurrentPdfBuffer) orbitRenderPdf(orbitCurrentPdfBuffer); };
  renderTextPane = function(extractedText) { textUnits = []; const target = $('textContent'); if (!target) return; target.innerHTML = ''; if (!String(extractedText || '').trim()) { target.innerHTML = '<div class="warning"><strong>No readable text found.</strong><br>This PDF may be scanned as images, protected, or missing a usable text map. Try an OCR version of the PDF.</div>'; setStatus('PDF saved, but no readable text was found.'); return; } if ($('modeBadge')) $('modeBadge').textContent = currentStyleLabel(); const hint = document.createElement('p'); hint.className = 'hint'; hint.textContent = currentHintText(); target.appendChild(hint); const chunks = buildChunks(extractedText, settings.style); let currentPage = 1; chunks.forEach((chunk) => { if (chunk.type === 'page') { const parsedPage = Number(String(chunk.text || '').replace(/[^0-9]/g, '')); if (Number.isFinite(parsedPage) && parsedPage > 0) currentPage = parsedPage; const br = document.createElement('span'); br.className = 'pageBreak'; br.textContent = chunk.text; target.appendChild(br); return; } const span = document.createElement('span'); span.className = 'unit kind-' + (chunk.kind || 'sentence') + (chunk.paragraphStart ? ' kind-paragraph-start' : ''); span.textContent = chunk.displayText + (chunk.kind === 'heading' ? '' : ' '); span.dataset.index = textUnits.length; span.onclick = () => { currentIndex = Number(span.dataset.index); setCurrent(currentIndex); scrollToIndex(currentIndex); }; target.appendChild(span); textUnits.push({ text: chunk.speechText || chunk.displayText, displayText: chunk.displayText, span, kind: chunk.kind || 'sentence', paragraphEnd: !!chunk.paragraphEnd, page: currentPage }); }); setStatus(textUnits.length + ' spoken sections ready in ' + currentStyleLabel() + ' mode.'); };
  setCurrent = function(i) { textUnits.forEach((unit) => unit.span.classList.remove('current')); if (textUnits[i]) { textUnits[i].span.classList.add('current'); if (textUnits[i].page) window.setOrbitPdfActivePage(textUnits[i].page, { scroll: false }); } };
})();
`;

const LIBRARY_GALLERY_JS = `
;(() => {
  let orbitLibraryView = 'gallery';
  const coverCache = new Map();
  function esc(value) { return String(value || '').replace(/[&<>\"]/g, (c) => ({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;' }[c])); }
  function sourceText(item) { return item.sourceUrl ? 'Online PDF' : 'Local PDF'; }
  function progressFor(progress) { return progress && Number.isInteger(progress.index) ? 'Resume section ' + (progress.index + 1) : 'Not started'; }
  function ensureToolbar() { const library = document.getElementById('library'); if (!library || document.querySelector('.orbitLibraryToolbar')) return; const toolbar = document.createElement('div'); toolbar.className = 'orbitLibraryToolbar'; toolbar.innerHTML = '<button class="orbitLibraryViewBtn active" type="button" data-view="gallery" aria-label="Gallery view">▦</button><button class="orbitLibraryViewBtn" type="button" data-view="list" aria-label="List view">☰</button><button class="orbitLibraryViewBtn" type="button" data-view="shelf" aria-label="Shelf view">▤</button>'; library.appendChild(toolbar); toolbar.querySelectorAll('button').forEach((button) => button.addEventListener('click', async () => { orbitLibraryView = button.dataset.view || 'gallery'; await renderList(); })); }
  function syncButtons() { document.querySelectorAll('.orbitLibraryViewBtn').forEach((button) => { const active = button.dataset.view === orbitLibraryView; button.classList.toggle('active', active); button.setAttribute('aria-pressed', String(active)); }); }
  const originalRenderList = renderList;
  renderList = async function() { await originalRenderList(); ensureToolbar(); syncButtons(); if (orbitLibraryView === 'gallery') await renderOrbitGallery(); else if (orbitLibraryView === 'shelf') document.getElementById('pdfList')?.classList.add('orbitShelfList'); };
  async function libraryEntries() { const items = (await getAllPdfs()).sort((a, b) => b.addedAt - a.addedAt); const entries = []; for (const item of items) entries.push({ item, progress: await storageGet(progressKey(item.id)) }); return entries; }
  async function renderOrbitGallery() { const list = document.getElementById('pdfList'); if (!list) return; const entries = await libraryEntries(); if (!entries.length) return; list.className = 'orbitGalleryList'; list.innerHTML = ''; const frontIndex = Math.max(0, entries.findIndex(({ item }) => item.id === currentPdfId)); const ordered = [entries[frontIndex]].concat(entries.slice(frontIndex + 1), entries.slice(0, frontIndex)).filter(Boolean); const shell = document.createElement('section'); shell.className = 'orbitGallery'; shell.innerHTML = '<div class="orbitGalleryStage"><div class="orbitGalleryStack"></div><button class="orbitGalleryFeature" type="button"></button></div><div class="orbitGalleryInfo"><h3></h3><p></p></div><div class="orbitGalleryRail"></div>'; const feature = shell.querySelector('.orbitGalleryFeature'); const stack = shell.querySelector('.orbitGalleryStack'); const infoTitle = shell.querySelector('h3'); const infoText = shell.querySelector('p'); const rail = shell.querySelector('.orbitGalleryRail'); let featured = ordered[0]; function show(entry) { featured = entry; feature.innerHTML = coverMarkup(entry, 'front'); infoTitle.textContent = entry.item.name; infoText.textContent = progressFor(entry.progress); shell.querySelectorAll('.orbitGalleryRailItem').forEach((button) => button.classList.toggle('active', button.dataset.pdfId === entry.item.id)); hydrateCovers(shell, entries); } feature.onclick = () => loadPdf(featured.item.id); ordered.slice(1, 6).forEach((entry, index) => { const button = document.createElement('button'); button.className = 'orbitGalleryStackCover'; button.type = 'button'; button.dataset.pdfId = entry.item.id; button.style.setProperty('--stack-index', String(index + 1)); button.innerHTML = coverMarkup(entry, 'stack'); button.onpointerenter = () => show(entry); button.onfocus = () => show(entry); button.onclick = () => loadPdf(entry.item.id); stack.appendChild(button); }); ordered.forEach((entry) => { const button = document.createElement('button'); button.className = 'orbitGalleryRailItem'; button.type = 'button'; button.dataset.pdfId = entry.item.id; button.innerHTML = coverMarkup(entry, 'rail'); button.onpointerenter = () => show(entry); button.onfocus = () => show(entry); button.onclick = () => loadPdf(entry.item.id); rail.appendChild(button); }); show(ordered[0]); list.appendChild(shell); hydrateCovers(shell, entries); }
  function coverMarkup(entry, size) { const marked = entry.progress && Number.isInteger(entry.progress.index); return '<span class="orbitBookCover ' + size + (marked ? ' marked' : '') + '" data-pdf-id="' + esc(entry.item.id) + '"><span class="orbitBookArt"></span><span class="orbitBookSpine"></span><span class="orbitBookSource">' + esc(sourceText(entry.item)) + '</span><strong>' + esc(entry.item.name) + '</strong><span class="orbitBookProgress">' + (marked ? '<b></b>' : '') + esc(progressFor(entry.progress)) + '</span></span>'; }
  function hydrateCovers(root, entries) { const map = new Map(entries.map((entry) => [entry.item.id, entry])); root.querySelectorAll('.orbitBookCover[data-pdf-id]').forEach((cover) => { const id = cover.dataset.pdfId; if (!id || cover.dataset.coverReady) return; const entry = map.get(id); if (!entry) return; cover.dataset.coverReady = 'pending'; renderCover(entry.item).then((url) => { if (!url) return; cover.style.setProperty('--cover-image', 'url("' + url + '")'); cover.dataset.coverReady = 'true'; }).catch(() => { cover.dataset.coverReady = 'failed'; }); }); }
  async function renderCover(item) { if (!item || !item.buffer) return ''; if (coverCache.has(item.id)) return coverCache.get(item.id); const promise = (async () => { const pdf = await pdfjsLib.getDocument({ data: item.buffer.slice(0), cMapUrl: new URL('./vendor/cmaps/', import.meta.url).toString(), cMapPacked: true, standardFontDataUrl: new URL('./vendor/standard_fonts/', import.meta.url).toString() }).promise; try { const page = await pdf.getPage(1); const base = page.getViewport({ scale: 1 }); const dpr = Math.min(window.devicePixelRatio || 1, 2); const viewport = page.getViewport({ scale: (360 / base.width) * dpr }); const canvas = document.createElement('canvas'); canvas.width = Math.max(1, Math.round(viewport.width)); canvas.height = Math.max(1, Math.round(viewport.height)); await page.render({ canvasContext: canvas.getContext('2d', { alpha: false }), viewport }).promise; return canvas.toDataURL('image/jpeg', .82); } finally { try { pdf.destroy(); } catch (_) {} } })(); coverCache.set(item.id, promise); return promise; }
  window.addEventListener('pageshow', () => { ensureToolbar(); if (orbitLibraryView === 'gallery') renderOrbitGallery(); });
})();
`;

const IOS_MARK_BRIDGE_JS = `
;(() => {
  if (!window.matchMedia || !window.matchMedia('(max-width: 700px)').matches) return;
  if (typeof DEFAULT_SETTINGS === 'object') DEFAULT_SETTINGS.pdfInverted = true;
  const getSelectedId = () => currentPdfId; const getCurrentIndex = () => currentIndex;
  function ensureOrbitHeader() { const brand = document.querySelector('.mobileTopLine strong'); if (brand) brand.textContent = 'Orbit'; }
  function ensureInvertDefault() { if (!settings || settings.pdfInverted === true) return; settings.pdfInverted = true; storageSet({ settings }); if (typeof applyInvertState === 'function') applyInvertState(); }
  async function markCurrentSpot() { const id = getSelectedId(); if (!id || !textUnits.length) { setStatus('Select a PDF first.'); syncDockMark(); return; } const index = Math.max(0, Math.min(getCurrentIndex() || 0, textUnits.length - 1)); await storageSet({ [progressKey(id)]: { index, updatedAt: Date.now(), style: settings.style } }); markReadThrough(index - 1); setStatus('Marked resume point: section ' + (index + 1) + '.'); renderList(); syncDockMark(); }
  const originalLoadSettings = loadSettings; loadSettings = function() { originalLoadSettings(); ensureOrbitHeader(); ensureInvertDefault(); };
  const originalSetCurrent = setCurrent; setCurrent = function(index) { originalSetCurrent(index); syncDockMark(); };
  const originalRenderTextPane = renderTextPane; renderTextPane = function(extractedText) { originalRenderTextPane(extractedText); ensureOrbitHeader(); syncDockMark(); };
  const originalLoadPdf = loadPdf; loadPdf = async function(id) { const result = await originalLoadPdf(id); ensureOrbitHeader(); syncDockMark(); return result; };
  window.addEventListener('pageshow', () => { ensureOrbitHeader(); ensureInvertDefault(); syncDockMark(); });
  document.getElementById('dockMark')?.addEventListener('click', (event) => { event.preventDefault(); markCurrentSpot(); }); ensureOrbitHeader();
  function syncDockMark() { const mark = document.getElementById('dockMark'); if (!mark) return; const hasPdf = !!getSelectedId(); mark.disabled = !hasPdf; mark.classList.toggle('active', hasPdf); mark.setAttribute('aria-disabled', String(!hasPdf)); mark.setAttribute('aria-label', hasPdf ? 'Mark resume point' : 'Select a PDF to mark'); }
})();
`;

function patchedTextResponse(response, patch, contentType) { return response.text().then((text) => new Response(text + '\n' + patch, { headers: { 'Content-Type': contentType, 'Cache-Control': 'no-store' } })); }
self.addEventListener('install', (event) => { self.skipWaiting(); event.waitUntil(caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL))); });
self.addEventListener('activate', (event) => { event.waitUntil(caches.keys().then((keys) => Promise.all(keys.filter((key) => key !== CACHE_NAME).map((key) => caches.delete(key)))).then(() => self.clients.claim())); });
self.addEventListener('fetch', (event) => {
  if (event.request.method !== 'GET') return; const url = new URL(event.request.url); if (url.origin !== self.location.origin) return;
  if (url.pathname.endsWith('/styles.css')) { event.respondWith(fetch(event.request).then((response) => patchedTextResponse(response, ORBIT_VIEWER_CSS + '\n' + LIBRARY_GALLERY_CSS, 'text/css')).catch(() => caches.match(event.request).then((cached) => cached ? patchedTextResponse(cached, ORBIT_VIEWER_CSS + '\n' + LIBRARY_GALLERY_CSS, 'text/css') : undefined))); return; }
  if (url.pathname.endsWith('/app.js')) { event.respondWith(fetch(event.request).then((response) => patchedTextResponse(response, ORBIT_VIEWER_JS + '\n' + LIBRARY_GALLERY_JS + '\n' + IOS_MARK_BRIDGE_JS, 'application/javascript')).catch(() => caches.match(event.request).then((cached) => cached ? patchedTextResponse(cached, ORBIT_VIEWER_JS + '\n' + LIBRARY_GALLERY_JS + '\n' + IOS_MARK_BRIDGE_JS, 'application/javascript') : undefined))); return; }
  event.respondWith(caches.match(event.request).then((cached) => { if (cached) return cached; return fetch(event.request).then((response) => { const copy = response.clone(); caches.open(CACHE_NAME).then((cache) => cache.put(event.request, copy)); return response; }).catch(() => caches.match('./index.html')); }));
});
